// TODO: 
// 1. fn: new todo note
// 2. fn: delete todo note
// 3. fn: modify todo note
// 4* fn: store note in folder

const TodoNote = (function(){
  const proto = {
    title: "",
    dueDate:
  }
  return Object.assign(Object.create(proto));
}
